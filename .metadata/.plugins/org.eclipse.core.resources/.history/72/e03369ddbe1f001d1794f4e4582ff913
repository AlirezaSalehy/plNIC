#pragma once

#include "Encryption.hpp"


// master first should start a connection (rx net id + enc method + timeout)
class VirtualConnection {
private:
	uint32_t timeOutMicSec;
	Enc::Encryption enc;
};
